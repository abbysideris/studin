раньше для всех секций использовали дивы. сейчас в ходе семантика
секция  -логически полный и конечный блок
пример тег section header footer
а внутри них можно открывать контейнеры - див класс контейнер

background cover помогает для адаптивности изображения, растягивание на всю ширину экрана

свойства шрифта цвета и другие некоторые наследуются дочерними элемантами до тех пор пока не будут переопределены - это хорошо для DRY( DONT REPEAT YOURSELF);

процесс верстки идет сверху вниз и слева направо.
меню всегда делаются списком

кстати у h2 по умолчанию есть отступы, их лучше обнулять 

конструкторы - ЗЛО!
плохой код, красивы только засчет дизайна. а плохой код вредит масштабируемости и продвижению. ради интереса надо бы посмотреть код сайта сделанного на конструкторе

макеты должны быть только в псд, из джепег нельзя верстать

оффтоп в евросеть можно сдать старый нетбук и что то взамен получить


блочные и строчные элементы
 строчные: img span a 
 блочные: div p h section article итд т.е их длина по умолчанию равна 100%



20.11.
Разбираемся с POSITION

немного о position


statis - все элементы со статик отображаются в исходном порядке сверху вниз. это значение по умолчанию ИСХОДНОГО ВНИМАНИЕ ИСХОДНОГО А НЕ РОДИТЕЛЯ
relative - изменение положения элемента относильно его исходного. элемент находится в общем потоке документа
absolute - изменение положения элемента относильно его исходного. элемент вылетает из общего потока
fixed -элемент вылетает из потока как absolute, привязывается к конкретным координатам(точке)
sticky - применяется для фиксации заголовка на одном месте, пока содержимое, к которому относится заголовок, прокручивается на странице.

Свойство position не наследуется.
Для изменения положения элементов по оси Z применяется свойство z-index, которое непосредственно связано со свойством position. z-index работает только для элементов, у которых position задано как relative, absolute или fixed.